# 프로젝트 로그

## 개요
- 개미 소굴 RTS(가제): 자원 채집 + 건설 + 유닛 강화 + 레이드 RTS
- 시점: 아이소메트릭(쿼터뷰), 싱글플레이 메인(멀티는 후순위)
- 엔진: Unity 6000.5.8f1, URP
- 프로젝트 루트: `E:\Git\ant`
- 버전관리: Git/GitHub (`github.com/ARHIENE/ant-colony-rts`, public)
- 기획 원본: Notion "게임 개발 노션 정리 > 기획(스펙 문서)" 페이지 참고
- 상세 작업 히스토리는 `changelog.md` 참고 — 이 파일은 "현재 상태 + 다음 할 일" 요약만 유지

## 한 줄 컨셉
플레이어는 개미 여왕의 지휘자가 되어 일개미로 자원을 캐고 소굴을 확장하며, 병정개미로 야생 몬스터를 상대하거나 다른 개미집을 약탈하고 거대 보스를 물량으로 레이드하는 RTS.

## 목표
**MVP가 아니라 Notion "기획(스펙 문서)" 페이지에 적힌 내용 전체 구현이 목표.** 기획이 개발 중 바뀌면 Notion 해당 하위 페이지를 교체(추가 아님)하고 관련 페이지도 같이 동기화할 것.

## 현재 상태 (세션 3 종료 시점 — 2026-09-05)

### 이번 세션에 새로 구현됨
- **보스 레이드**: `MiniBirdBoss`가 `(25, 8.5, 25)`에 실제 배치됨(3배 스케일). 원형→부채꼴→직선 순환 패턴, 빨간 텔레그래프(바닥 경고 장판) 정상 작동, 죽으면 패턴 정지+승리 이벤트. Play 실전 테스트로 탐지→캐스팅→데미지까지 전체 파이프라인 검증 완료(일개미 3마리 전멸시킨 것으로 확인)
  - 공격범위: 원형 반지름4 / 부채꼴 사거리6·90도 / 직선 길이8·폭3 (원본 수치 그대로, 한 차례 키웠다가 사용자 피드백으로 롤백)
  - 탐지 반경(`searchRadius`): 7 (원본 30에서 축소)
- **자원 유지비/반란**: `UpkeepManager`가 30초마다 전체 유닛 유지비(`UnitData.foodUpkeep`)를 식량에서 차감, 부족하면 무작위 개체 아사 또는 반란(→ `WildMonster`로 전환)
- **어택무브(A키)**: 병정개미 선택 후 A+클릭 — 적 클릭 시 직접 공격, 빈 땅 클릭 시 경로상 적 자동교전하며 이동
- **이동 확인 마커**: 우클릭 이동 시 클릭 지점에 초록 원판 표시(0.4초 축소 소멸)
- **Unity Layer**: `Ants`(9)/`Ground`(8) 신설, 개미 프리팹/지형에 각각 배정

### 중요 — 일개미 자동 채집이 이번 세션 막바지에 꺼짐 (다음 세션 최우선 확인)
사용자 요청으로 `WorkerAnt.TickIdle()`을 비워서 자동 채집 루프를 완전히 껐음. **현재 일개미는 우클릭으로 이동만 시킬 수 있고, 도착해도 자동으로 채집/반납하지 않음** — 사실상 자원 채집 루프가 막혀있는 상태. 다음 세션에서 대안(예: 자원노드를 우클릭하면 그 자리에서 수동으로 채집 시작하는 방식) 설계·구현 필요.

### unity-cli 브릿지 — 데드락 근본 원인 2가지 규명·수정 (이 프로젝트 코드 아님, 별도 정리)
- `refresh_assets`가 자기 응답을 보내는 도중 동기적으로 도메인 리로드를 유발해 응답을 영영 못 보내던 버그 → `EditorApplication.update` 기반 지연 콜백으로 수정 (`E:\Git\_tools\unity-cli` 로컬 패치, uncommitted)
- **`runInBackground: 0`이 "개미가 안 움직인다"의 진짜 원인이었음** — 창 포커스 없으면 브릿지는 응답하는데 Play 시뮬레이션 자체가 멈춤. `1`로 변경 완료
- Play 진입 시 강제 도메인 리로드하던 `EnterPlayModeOptions`도 끔
- 자세한 진단 과정은 메모리([[unity_cli_bridge_local_patch]], [[project_ant_teamimport_and_bridge_deadlock]]) 참고
- **규칙 변경**: 사용자가 직접 에디터를 조작 중인 신호가 있으면(예: 방금 Play를 눌렀다고 말함) 그 순간부터는 데드락이어도 자동 재시작하지 말고 먼저 물어볼 것

## 알아둘 것
- `Assets/_TeamImport/`는 SIMUL-TeaamProject(팀 원본 저장소, private) 반입본 — git에 절대 안 올라감(`.gitignore` 처리 완료)
- `Assets/Resources/Telegraph/*.prefab`은 우리 컴포넌트로 새로 만든 텔레그래프 프리팹(`.prefab`은 프로젝트 전역 규칙상 git 커밋 안 됨, `.meta`만 커밋됨) — 다음 세션에서 코드만 받아도 프리팹 자체는 없는 상태로 시작하니 재생성 필요할 수 있음(방법은 changelog.md 세션3 참고)
- NavMesh는 건물 구역만 좁게 베이크돼 있음(`NavMeshBakeArea`, Volume 60x60) — 플레이 영역 넓히면 재베이크 필요

## 다음 세션 할 일 (우선순위 순)
1. **일개미 채집 루프 복구/재설계** — 자동 채집을 껐으니 대체 수단(수동 채집 지시 등) 필요, 안 그러면 자원이 전혀 안 모임
2. 건물들을 400x400 지형 중앙 쪽으로 재배치 검토(현재 원점 구석에 몰려있음)
3. 선택된 유닛 정보 표시 UI(체력바 등)
4. 역할군별 병영 분리, 카테고리별 강화(연구소), 농사/낚시, 특수자원, 약탈, 방어전(적 침공 이벤트), 대형개미/특수배양소 — 기획서 대비 대부분 미구현 상태 유지

## 미정/논의 필요 항목 (기획서 5번)
- [ ] 개미 종족(불개미/베짜기개미 등) 다양화 여부
- [ ] 보스 종류 및 패턴 상세 설계(보스 스케일 다양화는 아직 MiniBird 하나뿐)
- [ ] 세션 길이 목표
- [ ] 세션 내 성장 vs 세션 간 영구 성장 분리 여부(기획서 3.5)
